---
description: Split the approved plan into smaller tasks and implement it with subagents
---
The plan looks good. Proceed with implementation. Split the work into small, well-defined tasks and delegate them to subagents. Run independent tasks in parallel, give each subagent clear scope and the context it needs, and avoid overlapping file edits. Coordinate dependent tasks, review and integrate the results, and run the required checks before reporting completion.
